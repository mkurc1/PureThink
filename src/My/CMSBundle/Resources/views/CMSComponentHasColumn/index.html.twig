{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CMSComponentHasColumn list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Columnlabel</th>
                <th>Class</th>
                <th>Isrequired</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('cmscomponenthascolumn_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.columnLabel }}</td>
                <td>{{ entity.class }}</td>
                <td>{{ entity.isRequired }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('cmscomponenthascolumn_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('cmscomponenthascolumn_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('cmscomponenthascolumn_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
