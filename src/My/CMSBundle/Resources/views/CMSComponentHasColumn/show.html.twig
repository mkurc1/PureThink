{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CMSComponentHasColumn</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Columnlabel</th>
                <td>{{ entity.columnLabel }}</td>
            </tr>
            <tr>
                <th>Class</th>
                <td>{{ entity.class }}</td>
            </tr>
            <tr>
                <th>Isrequired</th>
                <td>{{ entity.isRequired }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('cmscomponenthascolumn') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('cmscomponenthascolumn_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
