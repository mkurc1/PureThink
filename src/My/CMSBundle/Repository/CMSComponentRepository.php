<?php

namespace My\CMSBundle\Repository;

use Doctrine\ORM\EntityRepository;

/**
 * CMSComponentRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class CMSComponentRepository extends EntityRepository
{
    /**
     * Get components
     *
     * @param string $order
     * @param string $sequence
     * @param string $filtr
     * @param string $groupId
     * @return array
     */
	public function getComponents($order, $sequence, $filtr, $groupId)
	{
		$qb = $this->createQueryBuilder('a')
            ->leftJoin('a.series', 's')
            ->where('a.name LIKE :filtr')
            ->setParameter('filtr', '%'.$filtr.'%');

        if ($groupId) {
            $qb->andWhere('s.id = :groupId')
                ->setParameter('groupId', $groupId);
        }

        $qb->orderBy($order, $sequence);

		return $qb->getQuery();
	}
}
