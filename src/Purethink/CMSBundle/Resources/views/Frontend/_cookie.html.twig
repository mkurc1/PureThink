<div id="cookie">
    <div class="container">
        {% for key, cookie in entities if key != 'title' %}
            <h5>{{ cookie['nazwa'] }}</h5>
            <a class="close"><i class="fa fa-times"></i></a>

            <p>{{ cookie['tresc'] }} <a href="{{ path('article', { 'locale': locale, 'slug': cookie['adres'] }) }}">Dowiedz się więcej</a>.</p>
        {% endfor %}
    </div>
</div>